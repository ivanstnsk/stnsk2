export type TSizes = {
  content: {
    offset: number;
  };
  buttons: {
    mainCircleButtonSize: number;
  };
  menu: {
    buttonSize: number;
    buttonIconSize: number;
    buttonOffset: number;
  };
}
