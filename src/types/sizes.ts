export type TSizes = {
  content: {
    offsetHorizontal: number;
    offsetTop: number;
  };
  buttons: {
    mainCircleButtonSize: number;
  };
  menu: {
    buttonSize: number;
    buttonIconSize: number;
    buttonOffset: number;
  };
}
